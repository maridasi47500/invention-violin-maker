<%= form_with(model: epoque) do |form| %>
  <% if epoque.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(epoque.errors.count, "error") %> prohibited this epoque from being saved:</h2>

      <ul>
        <% epoque.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nom, style: "display: block" %>
    <%= form.text_field :nom %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
