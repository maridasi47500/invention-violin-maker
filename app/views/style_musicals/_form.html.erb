<%= form_with(model: style_musical) do |form| %>
  <% if style_musical.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(style_musical.errors.count, "error") %> prohibited this style_musical from being saved:</h2>

      <ul>
        <% style_musical.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nom, style: "display: block" %>
    <%= form.text_field :nom %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
