<%= form_with(model: corde) do |form| %>
  <% if corde.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(corde.errors.count, "error") %> prohibited this corde from being saved:</h2>

      <ul>
        <% corde.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nom, style: "display: block" %>
    <%= form.text_field :nom %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
