<%= form_with(model: string_spirit) do |form| %>
  <% if string_spirit.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(string_spirit.errors.count, "error") %> prohibited this string_spirit from being saved:</h2>

      <ul>
        <% string_spirit.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :corde_id, style: "display: block" %>
    <%= form.text_field :corde_id %>
  </div>

  <div>
    <%= form.label :epoque_id, style: "display: block" %>
    <%= form.text_field :epoque_id %>
  </div>

  <div>
    <%= form.label :compositeur_id, style: "display: block" %>
    <%= form.text_field :compositeur_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
