<%= form_with(model: violoniste) do |form| %>
  <% if violoniste.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(violoniste.errors.count, "error") %> prohibited this violoniste from being saved:</h2>

      <ul>
        <% violoniste.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nom, style: "display: block" %>
    <%= form.text_field :nom %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
